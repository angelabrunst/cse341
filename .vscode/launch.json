{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-node",
            "request": "launch",
            "name": "Debug NodeJS backend project",
          "skipFiles": ["<node_internals>/**"],
          "program": "${workspaceFolder}\\app.js"
        }
    ]
}